# WebView do Retangulo

App em React Native + Expo que carrega o site em uma WebView.

## Como rodar

```bash
npm install
npm start
```

## Gerar APK

1. Instale o EAS CLI:

```bash
npm install --global eas-cli
```

2. Gere a build Android:

```bash
npm run build
```
